import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.elegantfalcondrift',
  appName: 'elegant-falcon-drift',
  webDir: 'dist'
};

export default config;
